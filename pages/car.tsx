import React from "react";

import { Container, Header, Main, Footer, Cards, Meta } from "@components";

const Car: React.FC = () => {
  return (
    
    <Container>
      <Meta title="car" />
      {/* <Header /> */}
      <Main />
      <Cards />
      <Footer />
    </Container>
  );
};

export default Car;
